#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

Функция ПолучитьДанныеДляПечатнойФормыМ15(ПараметрыПечати, МассивОбъектов) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|   ПередачаВПереработку.Ссылка КАК ПередачаВПереработку,
	|   ПередачаВПереработку.ДокументПоступления
	|ПОМЕСТИТЬ втДокументыПоступления
	|ИЗ
	|   Документ.AMS_ПередачаВПереработку КАК ПередачаВПереработку
	|ГДЕ
	|   ПередачаВПереработку.Ссылка В (&МассивДокументов)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|   ДокументыПоступления.ПередачаВПереработку,
	|   ПриобретениеТоваровУслугТовары.Номенклатура,
	|   МАКСИМУМ(ПриобретениеТоваровУслугТовары.Цена) КАК Цена
	|ПОМЕСТИТЬ втЦены
	|ИЗ
	|   Документ.ПриобретениеТоваровУслуг.Товары КАК ПриобретениеТоваровУслугТовары
	|       ВНУТРЕННЕЕ СОЕДИНЕНИЕ втДокументыПоступления КАК ДокументыПоступления
	|       ПО ПриобретениеТоваровУслугТовары.Ссылка = ДокументыПоступления.ДокументПоступления
	|
	|СГРУППИРОВАТЬ ПО
	|   ПриобретениеТоваровУслугТовары.Номенклатура,
	|   ДокументыПоступления.ПередачаВПереработку
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|   Шапка.Ссылка                                           КАК Ссылка,
	|   Шапка.Номер                                            КАК Номер,
	|   Шапка.Дата                                             КАК Дата,
	|   Шапка.Дата                                             КАК ДатаСоставления,
	|   Шапка.Контрагент                                       КАК Контрагент,
	|   Шапка.Организация                                      КАК Организация,
	|   Шапка.Организация.Префикс                              КАК Префикс,
	|   Шапка.ДокументПоступления.Представление                КАК Основание,
	|   Шапка.Склад                                            КАК Склад,
	|   Шапка.Склад.Наименование                               КАК СкладНаименование,
	|   Шапка.Подразделение                                    КАК СтруктурноеПодразделение,
	|   Шапка.ДокументПоступления.БанковскийСчетОрганизации    КАК БанковскийСчетОрганизации,
	|   Шапка.ДокументПоступления.Валюта                       КАК Валюта,
	|   ИСТИНА                                                 КАК ЦенаВключаетНДС,
	|   Шапка.Организация._Руководитель                        КАК Руководитель,
	|   Шапка.Организация._ДолжностьРуководителя               КАК ДолжностьРуководителя,
	|   Шапка.Организация._ГлавныйБухгалтер                    КАК ГлавныйБухгалтер,
	|   Шапка.Организация._Кладовщик                           КАК Кладовщик,
	|   Шапка.Организация._ДолжностьКладовщика                 КАК ДолжностьКладовщика,
	|   ЛОЖЬ                                                   КАК ЭтоПередачаНаКомиссию
	|ИЗ
	|   Документ.AMS_ПередачаВПереработку КАК Шапка
	|ГДЕ
	|   Шапка.Ссылка В(&МассивДокументов)
	|
	|УПОРЯДОЧИТЬ ПО
	|   Ссылка
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|   Товары.Ссылка КАК Ссылка,
	|   Товары.Номенклатура,
	|   Товары.Номенклатура.Наименование КАК НоменклатураНаименование,
	|   Товары.Номенклатура.Код КАК ТоварКод,
	|   ВЫБОР
	|       КОГДА Товары.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка)
	|           ТОГДА Товары.Номенклатура.ЕдиницаИзмерения.Представление
	|       ИНАЧЕ Товары.Упаковка.Представление
	|   КОНЕЦ КАК ЕдиницаИзмеренияНаименование,
	|   ВЫБОР
	|       КОГДА Товары.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка)
	|           ТОГДА Товары.Номенклатура.ЕдиницаИзмерения.Код
	|       ИНАЧЕ Товары.Упаковка.ЕдиницаИзмерения.Код
	|   КОНЕЦ КАК ЕдиницаИзмеренияКод,
	|   Товары.Характеристика,
	|   Товары.Характеристика.Наименование ХарактеристикаНаименование,
	|   Товары.Упаковка,
	|   Товары.Количество,
	|   0 КАК КоличествоМест,
	|   Цены.Цена,
	|   ЕСТЬNULL(Цены.Цена, 0) * ЕСТЬNULL(Товары.Количество, 0) КАК Сумма,
	|   ЕСТЬNULL(Цены.Цена, 0) * ЕСТЬNULL(Товары.Количество, 0) * 18 / 118 КАК СуммаНДС,
	|   Товары.НомерСтроки КАК НомерСтроки,
	|   ЛОЖЬ КАК ЭтоВозвратнаяТара
	|ИЗ
	|   Документ.AMS_ПередачаВПереработку.Товары КАК Товары
	|       ВНУТРЕННЕЕ СОЕДИНЕНИЕ втЦены КАК Цены
	|       ПО Товары.Ссылка = Цены.ПередачаВПереработку
	|           И Товары.Номенклатура = Цены.Номенклатура
	|ГДЕ
	|   Товары.Ссылка В(&МассивДокументов)
	|
	|УПОРЯДОЧИТЬ ПО
	|   Ссылка,
	|   НомерСтроки
	|ИТОГИ ПО
	|   Ссылка";

	Запрос.УстановитьПараметр("МассивДокументов", МассивОбъектов);

	ТаблицаКурсовВалют = ТаблицаКурсовВалют();

	МассивРезультатов = Запрос.ВыполнитьПакет();
	РезультатПоШапке = МассивРезультатов[2];
	РезультатПоТабличнойЧасти = МассивРезультатов[3];

	СтруктураДанныхДляПечати = Новый Структура("РезультатПоШапке, РезультатПоТабличнойЧасти, ТаблицаКурсовВалют",
		РезультатПоШапке, РезультатПоТабличнойЧасти, ТаблицаКурсовВалют);

	Возврат СтруктураДанныхДляПечати;

КонецФункции

Функция ТаблицаКурсовВалют()
	ТаблицаКурсовВалют = Новый ТаблицаЗначений;
	ТаблицаКурсовВалют.Колонки.Добавить("Валюта", Новый ОписаниеТипов("СправочникСсылка.Валюты"));
	ТаблицаКурсовВалют.Колонки.Добавить("Дата", Новый ОписаниеТипов("Дата"));
	ТаблицаКурсовВалют.Колонки.Добавить("Курс", Новый ОписаниеТипов("Число"));
	ТаблицаКурсовВалют.Колонки.Добавить("Кратность", Новый ОписаниеТипов("Число"));

	НоваяСтрока = ТаблицаКурсовВалют.Добавить();
	НоваяСтрока.Валюта    = Константы.ВалютаРегламентированногоУчета.Получить();
	НоваяСтрока.Дата      = ТекущаяДатаСеанса();
	НоваяСтрока.Курс      = 1;
	НоваяСтрока.Кратность = 1;

	Возврат ТаблицаКурсовВалют;
КонецФункции

#КонецОбласти

#КонецЕсли