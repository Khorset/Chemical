
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ПроведениеБезОграничений = Параметры.ПроведениеБезОграничений;
	МассивСтрок              = Параметры.МассивСтрок;
	
	Элементы.ФормаРазрешитьОтгрузку.Доступность = ПроведениеБезОграничений;
	
	Для Каждого Стр Из МассивСтрок Цикл
		НовСтр = Табл.Добавить();
		ЗаполнитьЗначенияСвойств(НовСтр, Стр);
	КонецЦикла;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура РазрешитьОтгрузку(Команда)
	Закрыть(Истина);
КонецПроцедуры

#КонецОбласти

