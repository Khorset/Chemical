
&НаСервере
Процедура ОчиститьДанныеНаСервере()

	НаборЗаписей = РегистрыСведений.ДатаМобайл_УчетКМ.СоздатьНаборЗаписей();
		
	Попытка
	    НаборЗаписей.Записать(Истина);
	Исключение
	 	ДатаМобайл_ОбщийМодуль.ОбработатьИсключение(1, "ЗарегистрироватьДанныеВРегистрУчетКМ", ОписаниеОшибки());
	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура ОчиститьДанные(Команда)
	
	ОчиститьДанныеНаСервере(); 
	Элементы.Список.Обновить();
	
КонецПроцедуры
